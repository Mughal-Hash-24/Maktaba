{
  "slug": "os-named-pipes",
  "title": "OS Named Pipes",
  "tags": [
    "field/cs",
    "#field/cs",
    "#subject/os",
    "#concept/ipc/named_pipes"
  ],
  "htmlContent": "<p>Up to OS Notes</p>\n<blockquote>\n<p><strong>Prompt:</strong> \"Explain the concept of named pipe. Why do we need it. The syntax of it. The prototype of the named pipe function. Explain each of the parameters passed to it and why are these passed.\"\n<strong>Lens Applied:</strong> The Chief Engineer / The Constructivist</p>\n</blockquote>\n<h1>Technical breakdown: Named Pipes (FIFO)</h1>\n<h2>1. Surgical Definition (Internals)</h2>\n<p>A <strong>Named Pipe</strong> is a persistent, kernel-resident FIFO (First-In, First-Out) object that facilitates Inter-Process Communication (IPC). Unlike anonymous pipes, which are transient and restricted to parent-child process hierarchies, named pipes exist in the <strong>virtual filesystem</strong> (on Windows: <code>\\.\\pipe</code>). The kernel treats it as a shared memory buffer managed via file-like semantics. It provides a reliable byte-stream or message-oriented channel between a server process and one or more client processes.</p>\n<h2>2. The Laboratory (Proof of Concept)</h2>\n<p>On Windows (Win32 API), the server creates the pipe using <code>CreateNamedPipe</code>.</p>\n<pre><code class=\"hljs language-cpp\"><span class=\"hljs-meta\">#<span class=\"hljs-keyword\">include</span> <span class=\"hljs-string\">&#x3C;windows.h></span></span>\n<span class=\"hljs-meta\">#<span class=\"hljs-keyword\">include</span> <span class=\"hljs-string\">&#x3C;iostream></span></span>\n\n<span class=\"hljs-function\"><span class=\"hljs-type\">int</span> <span class=\"hljs-title\">main</span><span class=\"hljs-params\">()</span> </span>{\n    <span class=\"hljs-comment\">// 1. Create the pipe handle</span>\n    HANDLE hPipe = <span class=\"hljs-built_in\">CreateNamedPipe</span>(\n        <span class=\"hljs-string\">\"\\.\\pipe\\KybernetesPipe\"</span>, <span class=\"hljs-comment\">// Pipe name</span>\n        PIPE_ACCESS_DUPLEX,           <span class=\"hljs-comment\">// Read/Write access</span>\n        PIPE_TYPE_MESSAGE | PIPE_READMODE_MESSAGE | PIPE_WAIT, \n        <span class=\"hljs-number\">1</span>, <span class=\"hljs-number\">1024</span>, <span class=\"hljs-number\">1024</span>, <span class=\"hljs-number\">0</span>, <span class=\"hljs-literal\">NULL</span>);\n\n    <span class=\"hljs-keyword\">if</span> (hPipe == INVALID_HANDLE_VALUE) <span class=\"hljs-keyword\">return</span> <span class=\"hljs-number\">1</span>;\n\n    <span class=\"hljs-comment\">// 2. Wait for a client to connect</span>\n    std::cout &#x3C;&#x3C; <span class=\"hljs-string\">\"Waiting for client...\"</span> &#x3C;&#x3C; std::endl;\n    BOOL connected = <span class=\"hljs-built_in\">ConnectNamedPipe</span>(hPipe, <span class=\"hljs-literal\">NULL</span>);\n    \n    <span class=\"hljs-keyword\">if</span> (connected) {\n        <span class=\"hljs-type\">char</span> buffer[<span class=\"hljs-number\">1024</span>] = <span class=\"hljs-string\">\"Kybernetes System Online\"</span>;\n        DWORD written;\n        <span class=\"hljs-built_in\">WriteFile</span>(hPipe, buffer, <span class=\"hljs-built_in\">strlen</span>(buffer), &#x26;written, <span class=\"hljs-literal\">NULL</span>);\n    }\n\n    <span class=\"hljs-built_in\">CloseHandle</span>(hPipe);\n    <span class=\"hljs-keyword\">return</span> <span class=\"hljs-number\">0</span>;\n}\n</code></pre>\n<h2>3. Memory &#x26; System Context</h2>\n<ul>\n<li><strong>Stack/Heap:</strong> The <code>HANDLE</code> is a small stack-allocated pointer/index, but the actual buffer and pipe state live in the <strong>Kernel Paged Pool</strong>.</li>\n<li><strong>Cost:</strong> Low CPU overhead, but kernel memory is consumed for the buffers (<code>nOutBufferSize</code> + <code>nInBufferSize</code>). Context switching occurs during read/write if the buffer is empty/full.</li>\n<li><strong>Life-cycle:</strong> Persists as long as at least one handle to the pipe object exists in any process. Once all handles are closed, the kernel destroys the object.</li>\n</ul>\n<h2>4. Parameter Analysis (CreateNamedPipe)</h2>\n<ol>\n<li><strong>lpName:</strong> The unique identifier. Must follow <code>\\.\\pipe\\PipeName</code>. Necessary for clients to locate the resource.</li>\n<li><strong>dwOpenMode:</strong> Defines data flow (DUPLEX, INBOUND, OUTBOUND). Also enables <code>FILE_FLAG_OVERLAPPED</code> for asynchronous I/O.</li>\n<li><strong>dwPipeMode:</strong> Determines if data is read as a raw byte stream or discrete messages (<code>PIPE_TYPE_MESSAGE</code>).</li>\n<li><strong>nMaxInstances:</strong> Limits how many simultaneous client connections are allowed (1 to <code>PIPE_UNLIMITED_INSTANCES</code>). Prevents resource exhaustion.</li>\n<li><strong>nOut/InBufferSize:</strong> Advisory kernel buffer sizes. Passed to optimize memory usage based on expected payload size.</li>\n<li><strong>nDefaultTimeOut:</strong> Sets the default wait time for the <code>WaitNamedPipe</code> function if a client cannot immediately connect.</li>\n<li><strong>lpSecurityAttributes:</strong> Defines the Access Control List (ACL). Crucial for preventing unauthorized processes from hijacking the pipe.</li>\n</ol>\n<h2>5. Best Practices &#x26; Anti-Patterns</h2>\n<ul>\n<li><strong>Idiomatic:</strong> Use <code>PIPE_TYPE_MESSAGE</code> for structured data to avoid manual byte-framing.</li>\n<li><strong>Anti-Pattern:</strong> Hardcoding pipe names without unique prefixes, which can lead to \"Pipe Squatting\" security vulnerabilities. Always use strict ACLs in production.</li>\n</ul>",
  "rawMarkdown": "Up to OS Notes\n\n> **Prompt:** \"Explain the concept of named pipe. Why do we need it. The syntax of it. The prototype of the named pipe function. Explain each of the parameters passed to it and why are these passed.\"\n> **Lens Applied:** The Chief Engineer / The Constructivist\n\n# Technical breakdown: Named Pipes (FIFO)\n\n## 1. Surgical Definition (Internals)\nA **Named Pipe** is a persistent, kernel-resident FIFO (First-In, First-Out) object that facilitates Inter-Process Communication (IPC). Unlike anonymous pipes, which are transient and restricted to parent-child process hierarchies, named pipes exist in the **virtual filesystem** (on Windows: `\\.\\pipe`). The kernel treats it as a shared memory buffer managed via file-like semantics. It provides a reliable byte-stream or message-oriented channel between a server process and one or more client processes.\n\n## 2. The Laboratory (Proof of Concept)\nOn Windows (Win32 API), the server creates the pipe using `CreateNamedPipe`.\n\n```cpp\n#include <windows.h>\n#include <iostream>\n\nint main() {\n    // 1. Create the pipe handle\n    HANDLE hPipe = CreateNamedPipe(\n        \"\\.\\pipe\\KybernetesPipe\", // Pipe name\n        PIPE_ACCESS_DUPLEX,           // Read/Write access\n        PIPE_TYPE_MESSAGE | PIPE_READMODE_MESSAGE | PIPE_WAIT, \n        1, 1024, 1024, 0, NULL);\n\n    if (hPipe == INVALID_HANDLE_VALUE) return 1;\n\n    // 2. Wait for a client to connect\n    std::cout << \"Waiting for client...\" << std::endl;\n    BOOL connected = ConnectNamedPipe(hPipe, NULL);\n    \n    if (connected) {\n        char buffer[1024] = \"Kybernetes System Online\";\n        DWORD written;\n        WriteFile(hPipe, buffer, strlen(buffer), &written, NULL);\n    }\n\n    CloseHandle(hPipe);\n    return 0;\n}\n```\n\n## 3. Memory & System Context\n* **Stack/Heap:** The `HANDLE` is a small stack-allocated pointer/index, but the actual buffer and pipe state live in the **Kernel Paged Pool**.\n* **Cost:** Low CPU overhead, but kernel memory is consumed for the buffers (`nOutBufferSize` + `nInBufferSize`). Context switching occurs during read/write if the buffer is empty/full.\n* **Life-cycle:** Persists as long as at least one handle to the pipe object exists in any process. Once all handles are closed, the kernel destroys the object.\n\n## 4. Parameter Analysis (CreateNamedPipe)\n1. **lpName:** The unique identifier. Must follow `\\.\\pipe\\PipeName`. Necessary for clients to locate the resource.\n2. **dwOpenMode:** Defines data flow (DUPLEX, INBOUND, OUTBOUND). Also enables `FILE_FLAG_OVERLAPPED` for asynchronous I/O.\n3. **dwPipeMode:** Determines if data is read as a raw byte stream or discrete messages (`PIPE_TYPE_MESSAGE`).\n4. **nMaxInstances:** Limits how many simultaneous client connections are allowed (1 to `PIPE_UNLIMITED_INSTANCES`). Prevents resource exhaustion.\n5. **nOut/InBufferSize:** Advisory kernel buffer sizes. Passed to optimize memory usage based on expected payload size.\n6. **nDefaultTimeOut:** Sets the default wait time for the `WaitNamedPipe` function if a client cannot immediately connect.\n7. **lpSecurityAttributes:** Defines the Access Control List (ACL). Crucial for preventing unauthorized processes from hijacking the pipe.\n\n## 5. Best Practices & Anti-Patterns\n* **Idiomatic:** Use `PIPE_TYPE_MESSAGE` for structured data to avoid manual byte-framing.\n* **Anti-Pattern:** Hardcoding pipe names without unique prefixes, which can lead to \"Pipe Squatting\" security vulnerabilities. Always use strict ACLs in production.\n",
  "wordCount": 471,
  "backlinks": [],
  "relativePath": "10_University/Semester_04/Operating Systems/Notes/1.1.13 - OS Named Pipes.md",
  "filename": "1.1.13 - OS Named Pipes"
}
{
  "slug": "os-pipe-synchronization-and-blocking",
  "title": "OS Pipe Synchronization and Blocking",
  "tags": [
    "field/cs",
    "#field/cs",
    "#subject/os",
    "#concept/ipc/pipes/synchronization"
  ],
  "htmlContent": "<p>Up to OS Notes</p>\n<blockquote>\n<p><strong>Prompt:</strong> \"What if a parent wants to read what a child writes to the pipe but the issue is if parent's execution starts first it will read what? Garbage? throw error?\"\n<strong>Lens Applied:</strong> The Chief Engineer / First Principles</p>\n</blockquote>\n<h1>Deep Dive: Pipe Synchronization and Blocking I/O</h1>\n<h2>1. Ontological Definition</h2>\n<p>A pipe is a unidirectional communication channel provided by the kernel for Inter-Process Communication (IPC). It is implemented as a finite-sized circular buffer in kernel memory, managed by a pair of file descriptors: a read end (<code>fd[0]</code>) and a write end (<code>fd[1]</code>). It follows a strict First-In-First-Out (FIFO) discipline.</p>\n<h2>2. The Internal Mechanics (Under the Hood)</h2>\n<p>When a parent process calls <code>read()</code> on a pipe before the child has written anything, it does <strong>not</strong> read garbage, nor does it throw an error. Instead, it encounters the fundamental OS property of <strong>Blocking I/O</strong>.</p>\n<ul>\n<li><strong>The Blocking Mechanism:</strong> By default, file descriptors created by <code>pipe()</code> are in blocking mode. When <code>read()</code> is invoked on an empty pipe, the kernel checks the buffer's status. Finding it empty, it changes the process state from <code>TASK_RUNNING</code> to <code>TASK_INTERRUPTIBLE</code> (Sleep).</li>\n<li><strong>Context Switching:</strong> The kernel removes the parent from the CPU's run queue, allowing other processes to execute. The parent is now \"parked\" on a wait queue associated with that specific pipe.</li>\n<li><strong>The Wake-up:</strong> Once the child process executes <code>write()</code>, the kernel copies the data into the pipe's buffer. It then scans the pipe's wait queue and wakes up the sleeping reader. The parent is moved back to the run queue, and the <code>read()</code> call finally returns with the data.</li>\n<li><strong>Pseudo-code (Kernel-side):</strong>\n<pre><code class=\"hljs language-c\"><span class=\"hljs-type\">ssize_t</span> <span class=\"hljs-title function_\">pipe_read</span><span class=\"hljs-params\">(<span class=\"hljs-keyword\">struct</span> file *filp, <span class=\"hljs-type\">char</span> __user *buf, <span class=\"hljs-type\">size_t</span> count)</span> {\n    <span class=\"hljs-keyword\">while</span> (pipe_is_empty(pipe)) {\n        <span class=\"hljs-keyword\">if</span> (all_writers_closed(pipe)) <span class=\"hljs-keyword\">return</span> <span class=\"hljs-number\">0</span>; <span class=\"hljs-comment\">// EOF</span>\n        <span class=\"hljs-keyword\">if</span> (filp->f_flags &#x26; O_NONBLOCK) <span class=\"hljs-keyword\">return</span> -EAGAIN;\n        wait_event_interruptible(pipe->wait_queue, !pipe_is_empty(pipe));\n    }\n    <span class=\"hljs-keyword\">return</span> copy_to_user(buf, pipe->buffer, count);\n}\n</code></pre>\n</li>\n</ul>\n<h2>3. Systems Context &#x26; Anchoring (Analogy/C++)</h2>\n<p>Think of a pipe as a physical teller window at a bank. If you (the parent) arrive at the window before the teller (the child) has the documents ready, you don't receive \"garbage\" papers. You are told to wait in line. You stay there, inactive, until the teller places the documents in the tray.</p>\n<p>In C++, this is handled via the <code>unistd.h</code> syscalls. After <code>pipe(fd)</code> and <code>fork()</code>, the parent calls <code>read(fd[0], buffer, size)</code>. If the child hasn't called <code>write(fd[1], ...)</code> yet, the parent's execution thread simply pauses at that line of code.</p>\n<h2>4. Edge Cases &#x26; Constraints</h2>\n<ul>\n<li><strong>EAGAIN/EWOULDBLOCK:</strong> If the pipe was opened with the <code>O_NONBLOCK</code> flag, <code>read()</code> would return immediately with an error code instead of waiting. This is used in asynchronous event loops.</li>\n<li><strong>EOF (End of File):</strong> If the child closes the write end of the pipe without writing anything, the parent's <code>read()</code> will unblock and return <code>0</code>. This is the standard way to detect that communication has ended.</li>\n<li><strong>Pipe Capacity:</strong> If the child writes faster than the parent reads, and the 64KB buffer fills up, the <em>child</em> will block on <code>write()</code> until the parent clears space.</li>\n</ul>",
  "rawMarkdown": "Up to OS Notes\n\n> **Prompt:** \"What if a parent wants to read what a child writes to the pipe but the issue is if parent's execution starts first it will read what? Garbage? throw error?\"\n> **Lens Applied:** The Chief Engineer / First Principles\n\n# Deep Dive: Pipe Synchronization and Blocking I/O\n\n## 1. Ontological Definition\nA pipe is a unidirectional communication channel provided by the kernel for Inter-Process Communication (IPC). It is implemented as a finite-sized circular buffer in kernel memory, managed by a pair of file descriptors: a read end (`fd[0]`) and a write end (`fd[1]`). It follows a strict First-In-First-Out (FIFO) discipline.\n\n## 2. The Internal Mechanics (Under the Hood)\nWhen a parent process calls `read()` on a pipe before the child has written anything, it does **not** read garbage, nor does it throw an error. Instead, it encounters the fundamental OS property of **Blocking I/O**.\n\n*   **The Blocking Mechanism:** By default, file descriptors created by `pipe()` are in blocking mode. When `read()` is invoked on an empty pipe, the kernel checks the buffer's status. Finding it empty, it changes the process state from `TASK_RUNNING` to `TASK_INTERRUPTIBLE` (Sleep).\n*   **Context Switching:** The kernel removes the parent from the CPU's run queue, allowing other processes to execute. The parent is now \"parked\" on a wait queue associated with that specific pipe.\n*   **The Wake-up:** Once the child process executes `write()`, the kernel copies the data into the pipe's buffer. It then scans the pipe's wait queue and wakes up the sleeping reader. The parent is moved back to the run queue, and the `read()` call finally returns with the data.\n*   **Pseudo-code (Kernel-side):**\n    ```c\n    ssize_t pipe_read(struct file *filp, char __user *buf, size_t count) {\n        while (pipe_is_empty(pipe)) {\n            if (all_writers_closed(pipe)) return 0; // EOF\n            if (filp->f_flags & O_NONBLOCK) return -EAGAIN;\n            wait_event_interruptible(pipe->wait_queue, !pipe_is_empty(pipe));\n        }\n        return copy_to_user(buf, pipe->buffer, count);\n    }\n    ```\n\n## 3. Systems Context & Anchoring (Analogy/C++)\nThink of a pipe as a physical teller window at a bank. If you (the parent) arrive at the window before the teller (the child) has the documents ready, you don't receive \"garbage\" papers. You are told to wait in line. You stay there, inactive, until the teller places the documents in the tray. \n\nIn C++, this is handled via the `unistd.h` syscalls. After `pipe(fd)` and `fork()`, the parent calls `read(fd[0], buffer, size)`. If the child hasn't called `write(fd[1], ...)` yet, the parent's execution thread simply pauses at that line of code.\n\n## 4. Edge Cases & Constraints\n*   **EAGAIN/EWOULDBLOCK:** If the pipe was opened with the `O_NONBLOCK` flag, `read()` would return immediately with an error code instead of waiting. This is used in asynchronous event loops.\n*   **EOF (End of File):** If the child closes the write end of the pipe without writing anything, the parent's `read()` will unblock and return `0`. This is the standard way to detect that communication has ended.\n*   **Pipe Capacity:** If the child writes faster than the parent reads, and the 64KB buffer fills up, the *child* will block on `write()` until the parent clears space.\n",
  "wordCount": 530,
  "backlinks": [],
  "relativePath": "10_University/Semester_04/Operating Systems/Notes/1.1.12 - OS Pipe Synchronization and Blocking.md",
  "filename": "1.1.12 - OS Pipe Synchronization and Blocking"
}
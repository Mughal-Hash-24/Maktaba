{
  "slug": "sql-standard-aggregates-deep-dive",
  "title": "SQL Standard Aggregates Deep Dive",
  "tags": [
    "field/cs",
    "subject/database",
    "concept/sql/standard"
  ],
  "htmlContent": "<h1>SQL Standard Aggregates Deep Dive</h1>\n<p>Up to Database Systems Notes</p>\n<p>#concept #sql #aggregation #functions</p>\n<h2>1. The COUNT Function</h2>\n<blockquote>\n<p><strong>Prompt:</strong> \"Explain in detail the aggregate function of count with all possible cases and syntax and edge cases and default behaviours with detailed examples of each and make sure the concept is detailed like textbooks and the code examples are explained in depth. Numeric + Non-numeric\"\n<strong>Lens Applied:</strong> The Algorithmist</p>\n</blockquote>\n<h3>Definition</h3>\n<p>Returns the cardinality (number of items) of a set.</p>\n<h3>Variants &#x26; Syntax</h3>\n<ol>\n<li><strong><code>COUNT(*)</code></strong>: Counts <strong>all rows</strong>, including NULLs and duplicates. It measures the \"physical size\" of the table or group.\n<pre><code class=\"hljs language-sql\"><span class=\"hljs-keyword\">SELECT</span> <span class=\"hljs-built_in\">COUNT</span>(<span class=\"hljs-operator\">*</span>) <span class=\"hljs-keyword\">FROM</span> employees; <span class=\"hljs-comment\">-- Returns total staff count (e.g., 100)</span>\n</code></pre>\n</li>\n<li><strong><code>COUNT(column)</code></strong>: Counts only <strong>Non-NULL</strong> values in that specific column.\n<pre><code class=\"hljs language-sql\"><span class=\"hljs-keyword\">SELECT</span> <span class=\"hljs-built_in\">COUNT</span>(commission) <span class=\"hljs-keyword\">FROM</span> employees; \n<span class=\"hljs-comment\">-- If 20 people have NULL commission, this returns 80.</span>\n</code></pre>\n</li>\n<li><strong><code>COUNT(DISTINCT column)</code></strong>: Counts unique, Non-NULL values.\n<pre><code class=\"hljs language-sql\"><span class=\"hljs-keyword\">SELECT</span> <span class=\"hljs-built_in\">COUNT</span>(<span class=\"hljs-keyword\">DISTINCT</span> job_title) <span class=\"hljs-keyword\">FROM</span> employees; \n<span class=\"hljs-comment\">-- Returns how many unique roles exist (e.g., 5).</span>\n</code></pre>\n</li>\n</ol>\n<h3>Edge Cases</h3>\n<ul>\n<li><strong>All NULLs:</strong> <code>COUNT(null_column)</code> returns 0.</li>\n<li><strong>Non-Numeric:</strong> Works on Strings, Dates, etc. <code>COUNT(name)</code> counts employees with names.</li>\n</ul>\n<hr>\n<h2>2. The MAX Function</h2>\n<blockquote>\n<p><strong>Prompt:</strong> \"Explain in detail the aggregate function of max...\"\n<strong>Lens Applied:</strong> The Algorithmist</p>\n</blockquote>\n<h3>Definition</h3>\n<p>Returns the maximum value in a set.</p>\n<h3>Behavior by Data Type</h3>\n<ol>\n<li><strong>Numeric:</strong> Returns the largest number.\n<pre><code class=\"hljs language-sql\"><span class=\"hljs-keyword\">SELECT</span> <span class=\"hljs-built_in\">MAX</span>(salary) <span class=\"hljs-keyword\">FROM</span> employees; <span class=\"hljs-comment\">-- Highest paid employee</span>\n</code></pre>\n</li>\n<li><strong>String (Varchar):</strong> Returns the last value lexicographically (Alphabetical Z-A).\n<pre><code class=\"hljs language-sql\"><span class=\"hljs-keyword\">SELECT</span> <span class=\"hljs-built_in\">MAX</span>(name) <span class=\"hljs-keyword\">FROM</span> employees; <span class=\"hljs-comment\">-- \"Zack\" comes after \"Alice\"</span>\n</code></pre>\n</li>\n<li><strong>Date/Time:</strong> Returns the <strong>latest</strong> date (most recent).\n<pre><code class=\"hljs language-sql\"><span class=\"hljs-keyword\">SELECT</span> <span class=\"hljs-built_in\">MAX</span>(hire_date) <span class=\"hljs-keyword\">FROM</span> employees; <span class=\"hljs-comment\">-- The junior-most employee</span>\n</code></pre>\n</li>\n</ol>\n<hr>\n<h2>3. The SUM Function</h2>\n<blockquote>\n<p><strong>Prompt:</strong> \"Explain in detail the aggregate function of sum...\"\n<strong>Lens Applied:</strong> The Algorithmist</p>\n</blockquote>\n<h3>Definition</h3>\n<p>Returns the summation of all values in a set.</p>\n<h3>Constraints</h3>\n<ul>\n<li><strong>Numeric Only:</strong> Cannot <code>SUM</code> strings or dates. <code>SUM('A', 'B')</code> throws an error.</li>\n<li><strong>NULL Handling:</strong> <code>NULL</code> values are treated as 0 (ignored). <code>10 + NULL + 20</code> in scalar math is <code>NULL</code>, but in Aggregate <code>SUM</code>, it is <code>30</code>.</li>\n</ul>\n<h3>Example</h3>\n<pre><code class=\"hljs language-sql\"><span class=\"hljs-keyword\">SELECT</span> <span class=\"hljs-built_in\">SUM</span>(salary) <span class=\"hljs-keyword\">FROM</span> employees <span class=\"hljs-keyword\">WHERE</span> dept_id <span class=\"hljs-operator\">=</span> <span class=\"hljs-number\">10</span>;\n<span class=\"hljs-comment\">-- Returns total payroll for Department 10.</span>\n</code></pre>\n<hr>\n<h2>4. The AVG Function</h2>\n<blockquote>\n<p><strong>Prompt:</strong> \"Explain in detail the aggregate function of avg...\"\n<strong>Lens Applied:</strong> The Algorithmist</p>\n</blockquote>\n<h3>Definition</h3>\n<p>Returns the Arithmetic Mean. Internally computed as <code>SUM(col) / COUNT(col)</code>.</p>\n<h3>The NULL Trap (Critical Edge Case)</h3>\n<p><code>AVG</code> ignores NULLs completely. It does <strong>not</strong> treat them as zero.</p>\n<ul>\n<li><strong>Data:</strong> <code>{100, 200, NULL}</code></li>\n<li><strong>Calculation:</strong> <span class=\"katex\"><span class=\"katex-mathml\"><math xmlns=\"http://www.w3.org/1998/Math/MathML\"><semantics><mrow><mo stretchy=\"false\">(</mo><mn>100</mn><mo>+</mo><mn>200</mn><mo stretchy=\"false\">)</mo><mi mathvariant=\"normal\">/</mi><mn>2</mn><mo>=</mo><mn>150</mn></mrow><annotation encoding=\"application/x-tex\">(100 + 200) / 2 = 150</annotation></semantics></math></span><span class=\"katex-html\" aria-hidden=\"true\"><span class=\"base\"><span class=\"strut\" style=\"height:1em;vertical-align:-0.25em;\"></span><span class=\"mopen\">(</span><span class=\"mord\">100</span><span class=\"mspace\" style=\"margin-right:0.2222em;\"></span><span class=\"mbin\">+</span><span class=\"mspace\" style=\"margin-right:0.2222em;\"></span></span><span class=\"base\"><span class=\"strut\" style=\"height:1em;vertical-align:-0.25em;\"></span><span class=\"mord\">200</span><span class=\"mclose\">)</span><span class=\"mord\">/2</span><span class=\"mspace\" style=\"margin-right:0.2778em;\"></span><span class=\"mrel\">=</span><span class=\"mspace\" style=\"margin-right:0.2778em;\"></span></span><span class=\"base\"><span class=\"strut\" style=\"height:0.6444em;\"></span><span class=\"mord\">150</span></span></span></span>. (Divides by 2, not 3).</li>\n<li><strong>If you want to treat NULL as 0:</strong> You must use <code>COALESCE</code> or <code>NVL</code>.\n<pre><code class=\"hljs language-sql\"><span class=\"hljs-keyword\">SELECT</span> <span class=\"hljs-built_in\">AVG</span>(<span class=\"hljs-built_in\">COALESCE</span>(commission, <span class=\"hljs-number\">0</span>)) <span class=\"hljs-keyword\">FROM</span> employees;\n<span class=\"hljs-comment\">-- Now calculates (100 + 200 + 0) / 3 = 100.</span>\n</code></pre>\n</li>\n</ul>\n<hr>\n<h2>5. The MIN Function</h2>\n<blockquote>\n<p><strong>Prompt:</strong> \"Explain in detail the aggregate function of min...\"\n<strong>Lens Applied:</strong> The Algorithmist</p>\n</blockquote>\n<h3>Definition</h3>\n<p>Returns the minimum value in a set.</p>\n<h3>Behavior by Data Type</h3>\n<ol>\n<li><strong>Numeric:</strong> Returns the smallest number.\n<pre><code class=\"hljs language-sql\"><span class=\"hljs-keyword\">SELECT</span> <span class=\"hljs-built_in\">MIN</span>(salary) <span class=\"hljs-keyword\">FROM</span> employees; <span class=\"hljs-comment\">-- Lowest paid entry</span>\n</code></pre>\n</li>\n<li><strong>String:</strong> Returns the first value lexicographically (Alphabetical A-Z).\n<pre><code class=\"hljs language-sql\"><span class=\"hljs-keyword\">SELECT</span> <span class=\"hljs-built_in\">MIN</span>(name) <span class=\"hljs-keyword\">FROM</span> employees; <span class=\"hljs-comment\">-- \"Aaron\"</span>\n</code></pre>\n</li>\n<li><strong>Date/Time:</strong> Returns the <strong>earliest</strong> date (oldest).\n<pre><code class=\"hljs language-sql\"><span class=\"hljs-keyword\">SELECT</span> <span class=\"hljs-built_in\">MIN</span>(hire_date) <span class=\"hljs-keyword\">FROM</span> employees; <span class=\"hljs-comment\">-- The longest-serving employee</span>\n</code></pre>\n</li>\n</ol>",
  "rawMarkdown": "\n# SQL Standard Aggregates Deep Dive\n\nUp to Database Systems Notes\n\n#concept #sql #aggregation #functions\n\n## 1. The COUNT Function\n\n> **Prompt:** \"Explain in detail the aggregate function of count with all possible cases and syntax and edge cases and default behaviours with detailed examples of each and make sure the concept is detailed like textbooks and the code examples are explained in depth. Numeric + Non-numeric\"\n> **Lens Applied:** The Algorithmist\n\n### Definition\nReturns the cardinality (number of items) of a set.\n\n### Variants & Syntax\n1.  **`COUNT(*)`**: Counts **all rows**, including NULLs and duplicates. It measures the \"physical size\" of the table or group.\n    ```sql\n    SELECT COUNT(*) FROM employees; -- Returns total staff count (e.g., 100)\n    ```\n2.  **`COUNT(column)`**: Counts only **Non-NULL** values in that specific column.\n    ```sql\n    SELECT COUNT(commission) FROM employees; \n    -- If 20 people have NULL commission, this returns 80.\n    ```\n3.  **`COUNT(DISTINCT column)`**: Counts unique, Non-NULL values.\n    ```sql\n    SELECT COUNT(DISTINCT job_title) FROM employees; \n    -- Returns how many unique roles exist (e.g., 5).\n    ```\n\n### Edge Cases\n*   **All NULLs:** `COUNT(null_column)` returns 0.\n*   **Non-Numeric:** Works on Strings, Dates, etc. `COUNT(name)` counts employees with names.\n\n---\n\n## 2. The MAX Function\n\n> **Prompt:** \"Explain in detail the aggregate function of max...\"\n> **Lens Applied:** The Algorithmist\n\n### Definition\nReturns the maximum value in a set.\n\n### Behavior by Data Type\n1.  **Numeric:** Returns the largest number.\n    ```sql\n    SELECT MAX(salary) FROM employees; -- Highest paid employee\n    ```\n2.  **String (Varchar):** Returns the last value lexicographically (Alphabetical Z-A).\n    ```sql\n    SELECT MAX(name) FROM employees; -- \"Zack\" comes after \"Alice\"\n    ```\n3.  **Date/Time:** Returns the **latest** date (most recent).\n    ```sql\n    SELECT MAX(hire_date) FROM employees; -- The junior-most employee\n    ```\n\n---\n\n## 3. The SUM Function\n\n> **Prompt:** \"Explain in detail the aggregate function of sum...\"\n> **Lens Applied:** The Algorithmist\n\n### Definition\nReturns the summation of all values in a set.\n\n### Constraints\n*   **Numeric Only:** Cannot `SUM` strings or dates. `SUM('A', 'B')` throws an error.\n*   **NULL Handling:** `NULL` values are treated as 0 (ignored). `10 + NULL + 20` in scalar math is `NULL`, but in Aggregate `SUM`, it is `30`.\n\n### Example\n```sql\nSELECT SUM(salary) FROM employees WHERE dept_id = 10;\n-- Returns total payroll for Department 10.\n```\n\n---\n\n## 4. The AVG Function\n\n> **Prompt:** \"Explain in detail the aggregate function of avg...\"\n> **Lens Applied:** The Algorithmist\n\n### Definition\nReturns the Arithmetic Mean. Internally computed as `SUM(col) / COUNT(col)`.\n\n### The NULL Trap (Critical Edge Case)\n`AVG` ignores NULLs completely. It does **not** treat them as zero.\n*   **Data:** `{100, 200, NULL}`\n*   **Calculation:** $(100 + 200) / 2 = 150$. (Divides by 2, not 3).\n*   **If you want to treat NULL as 0:** You must use `COALESCE` or `NVL`.\n    ```sql\n    SELECT AVG(COALESCE(commission, 0)) FROM employees;\n    -- Now calculates (100 + 200 + 0) / 3 = 100.\n    ```\n\n---\n\n## 5. The MIN Function\n\n> **Prompt:** \"Explain in detail the aggregate function of min...\"\n> **Lens Applied:** The Algorithmist\n\n### Definition\nReturns the minimum value in a set.\n\n### Behavior by Data Type\n1.  **Numeric:** Returns the smallest number.\n    ```sql\n    SELECT MIN(salary) FROM employees; -- Lowest paid entry\n    ```\n2.  **String:** Returns the first value lexicographically (Alphabetical A-Z).\n    ```sql\n    SELECT MIN(name) FROM employees; -- \"Aaron\"\n    ```\n3.  **Date/Time:** Returns the **earliest** date (oldest).\n    ```sql\n    SELECT MIN(hire_date) FROM employees; -- The longest-serving employee\n    ```\n",
  "wordCount": 523,
  "backlinks": [],
  "relativePath": "10_University/Semester_04/Database Systems/Notes/1.1.8 - SQL Standard Aggregates Deep Dive.md",
  "filename": "1.1.8 - SQL Standard Aggregates Deep Dive"
}
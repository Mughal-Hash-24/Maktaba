{
  "slug": "sql-aggregation-case-study",
  "title": "SQL Aggregation Case Study",
  "tags": [
    "field/cs",
    "subject/database",
    "concept/sql/aggregation"
  ],
  "htmlContent": "<h1>SQL Aggregation Case Study</h1>\n<p>Up to Database Systems Notes</p>\n<p>#concept #sql #practice</p>\n<h2>1. Case Study: The \"TechStore\" Analytics</h2>\n<blockquote>\n<p><strong>Prompt:</strong> \"Define a detailed case study that forces the use of the 5 aggregate functions of SQL and then provide the solution of each part. There must be 6 parts. 1 for each and 6th one should implement each of the functions\"\n<strong>Lens Applied:</strong> The Architect / The Pragmatic Developer</p>\n</blockquote>\n<h3>Scenario</h3>\n<p>You are the Database Analyst for \"TechStore\". You have a table <code>Sales</code> with columns: <code>SaleID</code>, <code>Product</code>, <code>Category</code>, <code>Amount</code>, <code>SaleDate</code>.</p>\n<h3>Part 1: Traffic Analysis (COUNT)</h3>\n<p><strong>Question:</strong> How many individual sales transactions have occurred?</p>\n<pre><code class=\"hljs language-sql\"><span class=\"hljs-keyword\">SELECT</span> <span class=\"hljs-built_in\">COUNT</span>(<span class=\"hljs-operator\">*</span>) <span class=\"hljs-keyword\">AS</span> Total_Transactions <span class=\"hljs-keyword\">FROM</span> Sales;\n</code></pre>\n<h3>Part 2: Revenue Analysis (SUM)</h3>\n<p><strong>Question:</strong> What is the total revenue generated by the store?</p>\n<pre><code class=\"hljs language-sql\"><span class=\"hljs-keyword\">SELECT</span> <span class=\"hljs-built_in\">SUM</span>(Amount) <span class=\"hljs-keyword\">AS</span> Total_Revenue <span class=\"hljs-keyword\">FROM</span> Sales;\n</code></pre>\n<h3>Part 3: Peak Performance (MAX)</h3>\n<p><strong>Question:</strong> What was the most expensive item sold (highest sale amount)?</p>\n<pre><code class=\"hljs language-sql\"><span class=\"hljs-keyword\">SELECT</span> <span class=\"hljs-built_in\">MAX</span>(Amount) <span class=\"hljs-keyword\">AS</span> Highest_Ticket_Item <span class=\"hljs-keyword\">FROM</span> Sales;\n</code></pre>\n<h3>Part 4: Entry Level Analysis (MIN)</h3>\n<p><strong>Question:</strong> What was the cheapest item sold, and when was the first sale ever made?</p>\n<pre><code class=\"hljs language-sql\"><span class=\"hljs-keyword\">SELECT</span> <span class=\"hljs-built_in\">MIN</span>(Amount) <span class=\"hljs-keyword\">AS</span> Cheapest_Item, <span class=\"hljs-built_in\">MIN</span>(SaleDate) <span class=\"hljs-keyword\">AS</span> First_Sale_Day \n<span class=\"hljs-keyword\">FROM</span> Sales;\n</code></pre>\n<h3>Part 5: Average Basket Size (AVG)</h3>\n<p><strong>Question:</strong> What is the average value of a customer's purchase?</p>\n<pre><code class=\"hljs language-sql\"><span class=\"hljs-keyword\">SELECT</span> <span class=\"hljs-built_in\">AVG</span>(Amount) <span class=\"hljs-keyword\">AS</span> Average_Transaction_Value <span class=\"hljs-keyword\">FROM</span> Sales;\n</code></pre>\n<h3>Part 6: The Executive Dashboard (Combined)</h3>\n<p><strong>Question:</strong> Generate a single report showing Total Sales count, Total Revenue, Min, Max, and Average sale amount.</p>\n<pre><code class=\"hljs language-sql\"><span class=\"hljs-keyword\">SELECT</span> \n    <span class=\"hljs-built_in\">COUNT</span>(<span class=\"hljs-operator\">*</span>) <span class=\"hljs-keyword\">AS</span> \"Total Sales\",\n    <span class=\"hljs-built_in\">SUM</span>(Amount) <span class=\"hljs-keyword\">AS</span> \"Total Revenue\",\n    <span class=\"hljs-built_in\">MIN</span>(Amount) <span class=\"hljs-keyword\">AS</span> \"Lowest Sale\",\n    <span class=\"hljs-built_in\">MAX</span>(Amount) <span class=\"hljs-keyword\">AS</span> \"Highest Sale\",\n    <span class=\"hljs-built_in\">AVG</span>(Amount) <span class=\"hljs-keyword\">AS</span> \"Average Sale\"\n<span class=\"hljs-keyword\">FROM</span> Sales;\n</code></pre>\n<hr>\n<h2>2. Practice Problem: Clerk Salaries</h2>\n<blockquote>\n<p><strong>Prompt:</strong> \"Write an SQL code that sums the total salary being given to all the employees of type clerk\"\n<strong>Lens Applied:</strong> The Optimizationist</p>\n</blockquote>\n<h3>Solution</h3>\n<pre><code class=\"hljs language-sql\"><span class=\"hljs-keyword\">SELECT</span> <span class=\"hljs-built_in\">SUM</span>(salary) <span class=\"hljs-keyword\">AS</span> \"Total Clerk Payroll\"\n<span class=\"hljs-keyword\">FROM</span> employees\n<span class=\"hljs-keyword\">WHERE</span> job <span class=\"hljs-operator\">=</span> <span class=\"hljs-string\">'CLERK'</span>; <span class=\"hljs-comment\">/* Or job LIKE 'CLERK' */</span>\n</code></pre>\n<h3>Breakdown</h3>\n<ol>\n<li><strong>Filter First:</strong> The database engine first applies the <code>WHERE job = 'CLERK'</code> filter to isolate only the relevant rows.</li>\n<li><strong>Aggregate Second:</strong> It then passes the <code>salary</code> values of these filtered rows to the <code>SUM</code> accumulator.</li>\n<li><strong>Efficiency:</strong> This is much faster than summing everything and filtering later (which isn't even possible in standard SQL without subqueries).</li>\n</ol>",
  "rawMarkdown": "\n# SQL Aggregation Case Study\n\nUp to Database Systems Notes\n\n#concept #sql #practice\n\n## 1. Case Study: The \"TechStore\" Analytics\n\n> **Prompt:** \"Define a detailed case study that forces the use of the 5 aggregate functions of SQL and then provide the solution of each part. There must be 6 parts. 1 for each and 6th one should implement each of the functions\"\n> **Lens Applied:** The Architect / The Pragmatic Developer\n\n### Scenario\nYou are the Database Analyst for \"TechStore\". You have a table `Sales` with columns: `SaleID`, `Product`, `Category`, `Amount`, `SaleDate`.\n\n### Part 1: Traffic Analysis (COUNT)\n**Question:** How many individual sales transactions have occurred?\n```sql\nSELECT COUNT(*) AS Total_Transactions FROM Sales;\n```\n\n### Part 2: Revenue Analysis (SUM)\n**Question:** What is the total revenue generated by the store?\n```sql\nSELECT SUM(Amount) AS Total_Revenue FROM Sales;\n```\n\n### Part 3: Peak Performance (MAX)\n**Question:** What was the most expensive item sold (highest sale amount)?\n```sql\nSELECT MAX(Amount) AS Highest_Ticket_Item FROM Sales;\n```\n\n### Part 4: Entry Level Analysis (MIN)\n**Question:** What was the cheapest item sold, and when was the first sale ever made?\n```sql\nSELECT MIN(Amount) AS Cheapest_Item, MIN(SaleDate) AS First_Sale_Day \nFROM Sales;\n```\n\n### Part 5: Average Basket Size (AVG)\n**Question:** What is the average value of a customer's purchase?\n```sql\nSELECT AVG(Amount) AS Average_Transaction_Value FROM Sales;\n```\n\n### Part 6: The Executive Dashboard (Combined)\n**Question:** Generate a single report showing Total Sales count, Total Revenue, Min, Max, and Average sale amount.\n```sql\nSELECT \n    COUNT(*) AS \"Total Sales\",\n    SUM(Amount) AS \"Total Revenue\",\n    MIN(Amount) AS \"Lowest Sale\",\n    MAX(Amount) AS \"Highest Sale\",\n    AVG(Amount) AS \"Average Sale\"\nFROM Sales;\n```\n\n---\n\n## 2. Practice Problem: Clerk Salaries\n\n> **Prompt:** \"Write an SQL code that sums the total salary being given to all the employees of type clerk\"\n> **Lens Applied:** The Optimizationist\n\n### Solution\n```sql\nSELECT SUM(salary) AS \"Total Clerk Payroll\"\nFROM employees\nWHERE job = 'CLERK'; /* Or job LIKE 'CLERK' */\n```\n\n### Breakdown\n1.  **Filter First:** The database engine first applies the `WHERE job = 'CLERK'` filter to isolate only the relevant rows.\n2.  **Aggregate Second:** It then passes the `salary` values of these filtered rows to the `SUM` accumulator.\n3.  **Efficiency:** This is much faster than summing everything and filtering later (which isn't even possible in standard SQL without subqueries).\n",
  "wordCount": 374,
  "backlinks": [],
  "relativePath": "10_University/Semester_04/Database Systems/Notes/1.1.9 - SQL Aggregation Case Study.md",
  "filename": "1.1.9 - SQL Aggregation Case Study"
}
{
  "slug": "notes-sql-set-operations-1",
  "title": "SQL Set Operations",
  "tags": [
    "field/cs",
    "subject/database",
    "concept/sql/set-operations"
  ],
  "htmlContent": "<p>Up to Database Systems Notes</p>\n<blockquote>\n<p><strong>Prompt:</strong> \"What are set operations in SQL, there purpose, there use cases, there syntax and examples\"\n<strong>Lens Applied:</strong> The Chief Engineer / The Constructivist</p>\n</blockquote>\n<h1>Technical breakdown: SQL Set Operations</h1>\n<h2>1. Surgical Definition (Internals)</h2>\n<p>SQL Set Operations (UNION, INTERSECT, EXCEPT/MINUS) are the practical implementation of <strong>Relational Algebra</strong> set theory. Unlike JOINs, which combine columns horizontally based on a predicate, set operations combine result sets <strong>vertically</strong>.</p>\n<p>From a compiler's perspective, these are not just \"merging lists.\" The query optimizer treats set operations as <strong>multi-source inputs</strong> that must conform to a strict structural contract: identical column counts and compatible data types. For operations like <code>UNION</code> (which implies distinctness), the engine inserts a hidden <code>DISTINCT</code> operator, typically implemented via a <strong>Hash Aggregate</strong> or a <strong>Sort-Unique</strong> operation, to prune duplicates across the combined buffers.</p>\n<h2>2. The Laboratory (Proof of Concept)</h2>\n<p>To demonstrate the engine's behavior, we compare a standard <code>UNION</code> with its high-performance counterpart, <code>UNION ALL</code>.</p>\n<pre><code class=\"hljs language-sql\"><span class=\"hljs-comment\">-- Create two sets of IDs</span>\n<span class=\"hljs-keyword\">WITH</span> SetA <span class=\"hljs-keyword\">AS</span> (<span class=\"hljs-keyword\">SELECT</span> <span class=\"hljs-number\">1</span> <span class=\"hljs-keyword\">AS</span> id <span class=\"hljs-keyword\">UNION</span> <span class=\"hljs-keyword\">ALL</span> <span class=\"hljs-keyword\">SELECT</span> <span class=\"hljs-number\">2</span>),\n     SetB <span class=\"hljs-keyword\">AS</span> (<span class=\"hljs-keyword\">SELECT</span> <span class=\"hljs-number\">2</span> <span class=\"hljs-keyword\">AS</span> id <span class=\"hljs-keyword\">UNION</span> <span class=\"hljs-keyword\">ALL</span> <span class=\"hljs-keyword\">SELECT</span> <span class=\"hljs-number\">3</span>)\n\n<span class=\"hljs-comment\">-- 1. UNION: Combines and removes duplicates (Costly Sort/Hash)</span>\n<span class=\"hljs-keyword\">SELECT</span> id <span class=\"hljs-keyword\">FROM</span> SetA\n<span class=\"hljs-keyword\">UNION</span>\n<span class=\"hljs-keyword\">SELECT</span> id <span class=\"hljs-keyword\">FROM</span> SetB; <span class=\"hljs-comment\">-- Output: 1, 2, 3</span>\n\n<span class=\"hljs-comment\">-- 2. INTERSECT: Finds the overlap (Logic: A ∩ B)</span>\n<span class=\"hljs-keyword\">SELECT</span> id <span class=\"hljs-keyword\">FROM</span> SetA\n<span class=\"hljs-keyword\">INTERSECT</span>\n<span class=\"hljs-keyword\">SELECT</span> id <span class=\"hljs-keyword\">FROM</span> SetB; <span class=\"hljs-comment\">-- Output: 2</span>\n\n<span class=\"hljs-comment\">-- 3. EXCEPT (MINUS in Oracle): Difference (Logic: A - B)</span>\n<span class=\"hljs-keyword\">SELECT</span> id <span class=\"hljs-keyword\">FROM</span> SetA\n<span class=\"hljs-keyword\">EXCEPT</span>\n<span class=\"hljs-keyword\">SELECT</span> id <span class=\"hljs-keyword\">FROM</span> SetB; <span class=\"hljs-comment\">-- Output: 1</span>\n</code></pre>\n<h2>3. Memory &#x26; System Context</h2>\n<ul>\n<li><strong>Stack/Heap:</strong> SQL engines use <strong>TempDB</strong> or <strong>Work Tables</strong> in memory (or disk if the set exceeds the buffer pool) to perform the deduplication required by <code>UNION</code>, <code>INTERSECT</code>, and <code>EXCEPT</code>.</li>\n<li><strong>Cost:</strong> <code>UNION ALL</code> is the \"zero-cost\" operator because it simply concatenates stream pointers. <code>UNION</code> introduces <span class=\"katex\"><span class=\"katex-mathml\"><math xmlns=\"http://www.w3.org/1998/Math/MathML\"><semantics><mrow><mi>O</mi><mo stretchy=\"false\">(</mo><mi>N</mi><mi>log</mi><mo>⁡</mo><mi>N</mi><mo stretchy=\"false\">)</mo></mrow><annotation encoding=\"application/x-tex\">O(N \\log N)</annotation></semantics></math></span><span class=\"katex-html\" aria-hidden=\"true\"><span class=\"base\"><span class=\"strut\" style=\"height:1em;vertical-align:-0.25em;\"></span><span class=\"mord mathnormal\" style=\"margin-right:0.0278em;\">O</span><span class=\"mopen\">(</span><span class=\"mord mathnormal\" style=\"margin-right:0.109em;\">N</span><span class=\"mspace\" style=\"margin-right:0.1667em;\"></span><span class=\"mop\">lo<span style=\"margin-right:0.0139em;\">g</span></span><span class=\"mspace\" style=\"margin-right:0.1667em;\"></span><span class=\"mord mathnormal\" style=\"margin-right:0.109em;\">N</span><span class=\"mclose\">)</span></span></span></span> complexity due to the required sorting or <span class=\"katex\"><span class=\"katex-mathml\"><math xmlns=\"http://www.w3.org/1998/Math/MathML\"><semantics><mrow><mi>O</mi><mo stretchy=\"false\">(</mo><mi>N</mi><mo stretchy=\"false\">)</mo></mrow><annotation encoding=\"application/x-tex\">O(N)</annotation></semantics></math></span><span class=\"katex-html\" aria-hidden=\"true\"><span class=\"base\"><span class=\"strut\" style=\"height:1em;vertical-align:-0.25em;\"></span><span class=\"mord mathnormal\" style=\"margin-right:0.0278em;\">O</span><span class=\"mopen\">(</span><span class=\"mord mathnormal\" style=\"margin-right:0.109em;\">N</span><span class=\"mclose\">)</span></span></span></span> for hashing to ensure uniqueness.</li>\n<li><strong>Life-cycle:</strong> The execution plan usually involves a \"Concatenation\" operator followed by a \"Sort\" or \"Hash Match\" depending on the cardinality estimates.</li>\n</ul>\n<h2>4. Best Practices &#x26; Anti-Patterns</h2>\n<ul>\n<li><strong>The \"Union All\" Default:</strong> Always prefer <code>UNION ALL</code> unless business logic strictly requires unique rows. It avoids the massive CPU/IO overhead of deduplication.</li>\n<li><strong>Column Alignment:</strong> Ensure data types match exactly (e.g., <code>VARCHAR</code> vs <code>NVARCHAR</code> can trigger implicit conversions that break index usage).</li>\n<li><strong>Predicate Pushing:</strong> Set operations often act as an optimization barrier. Try to apply <code>WHERE</code> filters <em>inside</em> each subquery rather than on the final combined result set to reduce memory pressure.</li>\n</ul>",
  "rawMarkdown": "\nUp to Database Systems Notes\n\n> **Prompt:** \"What are set operations in SQL, there purpose, there use cases, there syntax and examples\"\n> **Lens Applied:** The Chief Engineer / The Constructivist\n\n# Technical breakdown: SQL Set Operations\n\n## 1. Surgical Definition (Internals)\nSQL Set Operations (UNION, INTERSECT, EXCEPT/MINUS) are the practical implementation of **Relational Algebra** set theory. Unlike JOINs, which combine columns horizontally based on a predicate, set operations combine result sets **vertically**.\n\nFrom a compiler's perspective, these are not just \"merging lists.\" The query optimizer treats set operations as **multi-source inputs** that must conform to a strict structural contract: identical column counts and compatible data types. For operations like `UNION` (which implies distinctness), the engine inserts a hidden `DISTINCT` operator, typically implemented via a **Hash Aggregate** or a **Sort-Unique** operation, to prune duplicates across the combined buffers.\n\n## 2. The Laboratory (Proof of Concept)\nTo demonstrate the engine's behavior, we compare a standard `UNION` with its high-performance counterpart, `UNION ALL`.\n\n```sql\n-- Create two sets of IDs\nWITH SetA AS (SELECT 1 AS id UNION ALL SELECT 2),\n     SetB AS (SELECT 2 AS id UNION ALL SELECT 3)\n\n-- 1. UNION: Combines and removes duplicates (Costly Sort/Hash)\nSELECT id FROM SetA\nUNION\nSELECT id FROM SetB; -- Output: 1, 2, 3\n\n-- 2. INTERSECT: Finds the overlap (Logic: A ∩ B)\nSELECT id FROM SetA\nINTERSECT\nSELECT id FROM SetB; -- Output: 2\n\n-- 3. EXCEPT (MINUS in Oracle): Difference (Logic: A - B)\nSELECT id FROM SetA\nEXCEPT\nSELECT id FROM SetB; -- Output: 1\n```\n\n## 3. Memory & System Context\n* **Stack/Heap:** SQL engines use **TempDB** or **Work Tables** in memory (or disk if the set exceeds the buffer pool) to perform the deduplication required by `UNION`, `INTERSECT`, and `EXCEPT`.\n* **Cost:** `UNION ALL` is the \"zero-cost\" operator because it simply concatenates stream pointers. `UNION` introduces $O(N \\log N)$ complexity due to the required sorting or $O(N)$ for hashing to ensure uniqueness.\n* **Life-cycle:** The execution plan usually involves a \"Concatenation\" operator followed by a \"Sort\" or \"Hash Match\" depending on the cardinality estimates.\n\n## 4. Best Practices & Anti-Patterns\n* **The \"Union All\" Default:** Always prefer `UNION ALL` unless business logic strictly requires unique rows. It avoids the massive CPU/IO overhead of deduplication.\n* **Column Alignment:** Ensure data types match exactly (e.g., `VARCHAR` vs `NVARCHAR` can trigger implicit conversions that break index usage).\n* **Predicate Pushing:** Set operations often act as an optimization barrier. Try to apply `WHERE` filters *inside* each subquery rather than on the final combined result set to reduce memory pressure.\n",
  "wordCount": 424,
  "backlinks": [],
  "relativePath": "10_University/Semester_04/Database Systems/Notes/1.1.23 - SQL Set Operations.md",
  "filename": "1.1.23 - SQL Set Operations"
}
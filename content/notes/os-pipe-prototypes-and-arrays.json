{
  "slug": "os-pipe-prototypes-and-arrays",
  "title": "OS Pipe Prototypes and Arrays",
  "tags": [
    "field/cs",
    "#field/cs",
    "#subject/os",
    "#concept/ipc/pipes"
  ],
  "htmlContent": "<p>Up to OS Notes</p>\n<blockquote>\n<p><strong>Prompt:</strong> \"Why do we create memory array named fd and then convert it to a pipe in inter-process communication. What's the prototype of pipe functions and what does it return.\"\n<strong>Lens Applied:</strong> The Chief Engineer / The Constructivist</p>\n</blockquote>\n<h1>Technical breakdown: The Unix Pipe (<code>pipe()</code>)</h1>\n<h2>1. Surgical Definition (Internals)</h2>\n<p>From the kernel's perspective, a pipe is a <strong>unidirectional data channel</strong> residing in kernel memory. We start by declaring an array <code>int fd[2]</code>—this is merely a placeholder on the process's <strong>stack</strong> for two integers.</p>\n<p>When the <code>pipe(fd)</code> system call is executed, the Kernel performs the following:</p>\n<ol>\n<li><strong>Buffer Allocation:</strong> It allocates a circular buffer (typically 64KB on modern Linux) in the kernel space.</li>\n<li><strong>VFS Inode Creation:</strong> It creates a \"virtual file\" that points to this buffer.</li>\n<li><strong>Descriptor Assignment:</strong> It scans the process's <strong>File Descriptor Table</strong> and grabs the two lowest available indices.</li>\n<li><strong>Mapping:</strong> It populates <code>fd[0]</code> with the index for the <em>read end</em> and <code>fd[1]</code> with the index for the <em>write end</em>.</li>\n</ol>\n<p>The \"conversion\" isn't changing the array; it's the Kernel using the array as a return-by-pointer mechanism to give the user-space process handles to a kernel-managed resource.</p>\n<h2>2. The Laboratory (Proof of Concept)</h2>\n<p>This snippet demonstrates how the Kernel populates the <code>fd</code> array and how the parent-child relationship (via <code>fork</code>) allows the descriptors to be shared across process boundaries.</p>\n<pre><code class=\"hljs language-c\"><span class=\"hljs-meta\">#<span class=\"hljs-keyword\">include</span> <span class=\"hljs-string\">&#x3C;stdio.h></span></span>\n<span class=\"hljs-meta\">#<span class=\"hljs-keyword\">include</span> <span class=\"hljs-string\">&#x3C;unistd.h></span></span>\n\n<span class=\"hljs-type\">int</span> <span class=\"hljs-title function_\">main</span><span class=\"hljs-params\">()</span> {\n    <span class=\"hljs-type\">int</span> fd[<span class=\"hljs-number\">2</span>]; <span class=\"hljs-comment\">// Stack allocation of 8 bytes (2 * sizeof(int))</span>\n    \n    <span class=\"hljs-keyword\">if</span> (pipe(fd) == <span class=\"hljs-number\">-1</span>) {\n        perror(<span class=\"hljs-string\">\"pipe\"</span>);\n        <span class=\"hljs-keyword\">return</span> <span class=\"hljs-number\">1</span>;\n    }\n\n    <span class=\"hljs-built_in\">printf</span>(<span class=\"hljs-string\">\"Read FD: %d, Write FD: %d\n\"</span>, fd[<span class=\"hljs-number\">0</span>], fd[<span class=\"hljs-number\">1</span>]);\n\n    <span class=\"hljs-keyword\">if</span> (fork() == <span class=\"hljs-number\">0</span>) { <span class=\"hljs-comment\">// Child Process</span>\n        close(fd[<span class=\"hljs-number\">1</span>]); <span class=\"hljs-comment\">// Close unused write end</span>\n        <span class=\"hljs-type\">char</span> buf[<span class=\"hljs-number\">12</span>];\n        read(fd[<span class=\"hljs-number\">0</span>], buf, <span class=\"hljs-number\">12</span>);\n        <span class=\"hljs-built_in\">printf</span>(<span class=\"hljs-string\">\"Child received: %s\n\"</span>, buf);\n        close(fd[<span class=\"hljs-number\">0</span>]);\n    } <span class=\"hljs-keyword\">else</span> { <span class=\"hljs-comment\">// Parent Process</span>\n        close(fd[<span class=\"hljs-number\">0</span>]); <span class=\"hljs-comment\">// Close unused read end</span>\n        write(fd[<span class=\"hljs-number\">1</span>], <span class=\"hljs-string\">\"Hello Pipe\"</span>, <span class=\"hljs-number\">11</span>);\n        close(fd[<span class=\"hljs-number\">1</span>]);\n    }\n    <span class=\"hljs-keyword\">return</span> <span class=\"hljs-number\">0</span>;\n}\n</code></pre>\n<h2>3. Memory &#x26; System Context</h2>\n<ul>\n<li><strong>Stack/Heap:</strong> The <code>fd</code> array lives on the <strong>stack</strong>. However, the actual data transit occurs in <strong>kernel-space memory</strong> (non-swappable).</li>\n<li><strong>Cost:</strong> Low overhead. The primary cost is the context switch from User Mode to Kernel Mode for the <code>read</code>/<code>write</code> system calls and the memory copy from user-space to kernel-space.</li>\n<li><strong>Life-cycle:</strong> The pipe exists as long as at least one file descriptor (in any process) points to it. Once all FDs for both ends are closed, the kernel deallocates the buffer.</li>\n</ul>\n<h2>4. Best Practices &#x26; Anti-Patterns</h2>\n<ul>\n<li><strong>The \"Half-Close\" Rule:</strong> Always close the unused end in each process. If the reader doesn't close the write end, <code>read()</code> will block indefinitely even if the actual writer is done, because the kernel sees a potential writer (the reader itself) still exists.</li>\n<li><strong>Return Values:</strong>\n<ul>\n<li><strong>Prototype:</strong> <code>int pipe(int pipefd[2]);</code></li>\n<li><strong>Success:</strong> Returns <code>0</code>.</li>\n<li><strong>Failure:</strong> Returns <code>-1</code>, setting <code>errno</code> (e.g., <code>EMFILE</code> if the process hit its descriptor limit).</li>\n</ul>\n</li>\n</ul>",
  "rawMarkdown": "Up to OS Notes\n\n> **Prompt:** \"Why do we create memory array named fd and then convert it to a pipe in inter-process communication. What's the prototype of pipe functions and what does it return.\"\n> **Lens Applied:** The Chief Engineer / The Constructivist\n\n# Technical breakdown: The Unix Pipe (`pipe()`)\n\n## 1. Surgical Definition (Internals)\nFrom the kernel's perspective, a pipe is a **unidirectional data channel** residing in kernel memory. We start by declaring an array `int fd[2]`—this is merely a placeholder on the process's **stack** for two integers.\n\nWhen the `pipe(fd)` system call is executed, the Kernel performs the following:\n1. **Buffer Allocation:** It allocates a circular buffer (typically 64KB on modern Linux) in the kernel space.\n2. **VFS Inode Creation:** It creates a \"virtual file\" that points to this buffer.\n3. **Descriptor Assignment:** It scans the process's **File Descriptor Table** and grabs the two lowest available indices.\n4. **Mapping:** It populates `fd[0]` with the index for the *read end* and `fd[1]` with the index for the *write end*.\n\nThe \"conversion\" isn't changing the array; it's the Kernel using the array as a return-by-pointer mechanism to give the user-space process handles to a kernel-managed resource.\n\n## 2. The Laboratory (Proof of Concept)\nThis snippet demonstrates how the Kernel populates the `fd` array and how the parent-child relationship (via `fork`) allows the descriptors to be shared across process boundaries.\n```c\n#include <stdio.h>\n#include <unistd.h>\n\nint main() {\n    int fd[2]; // Stack allocation of 8 bytes (2 * sizeof(int))\n    \n    if (pipe(fd) == -1) {\n        perror(\"pipe\");\n        return 1;\n    }\n\n    printf(\"Read FD: %d, Write FD: %d\n\", fd[0], fd[1]);\n\n    if (fork() == 0) { // Child Process\n        close(fd[1]); // Close unused write end\n        char buf[12];\n        read(fd[0], buf, 12);\n        printf(\"Child received: %s\n\", buf);\n        close(fd[0]);\n    } else { // Parent Process\n        close(fd[0]); // Close unused read end\n        write(fd[1], \"Hello Pipe\", 11);\n        close(fd[1]);\n    }\n    return 0;\n}\n```\n\n## 3. Memory & System Context\n* **Stack/Heap:** The `fd` array lives on the **stack**. However, the actual data transit occurs in **kernel-space memory** (non-swappable).\n* **Cost:** Low overhead. The primary cost is the context switch from User Mode to Kernel Mode for the `read`/`write` system calls and the memory copy from user-space to kernel-space.\n* **Life-cycle:** The pipe exists as long as at least one file descriptor (in any process) points to it. Once all FDs for both ends are closed, the kernel deallocates the buffer.\n\n## 4. Best Practices & Anti-Patterns\n* **The \"Half-Close\" Rule:** Always close the unused end in each process. If the reader doesn't close the write end, `read()` will block indefinitely even if the actual writer is done, because the kernel sees a potential writer (the reader itself) still exists.\n* **Return Values:**\n  * **Prototype:** `int pipe(int pipefd[2]);`\n  * **Success:** Returns `0`.\n  * **Failure:** Returns `-1`, setting `errno` (e.g., `EMFILE` if the process hit its descriptor limit).\n",
  "wordCount": 499,
  "backlinks": [],
  "relativePath": "10_University/Semester_04/Operating Systems/Notes/1.1.10 - OS Pipe Prototypes and Arrays.md",
  "filename": "1.1.10 - OS Pipe Prototypes and Arrays"
}
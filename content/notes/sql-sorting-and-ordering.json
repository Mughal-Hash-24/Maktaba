{
  "slug": "sql-sorting-and-ordering",
  "title": "SQL Sorting and Ordering",
  "tags": [
    "field/cs",
    "subject/database",
    "concept/sql/sorting"
  ],
  "htmlContent": "<h1>SQL Sorting and Ordering</h1>\n<p>Up to Database Systems Notes</p>\n<p>#concept #sql #sorting</p>\n<h2>1. Sorted Data Retrieval</h2>\n<blockquote>\n<p><strong>Prompt:</strong> \"Write an SQL code to view the records in increasing order of salary, then descending order. Give me explanations of both. What other keywords and commands are there for sorting retrieval of data\"\n<strong>Lens Applied:</strong> The Optimizationist / The Chief Engineer</p>\n</blockquote>\n<h3>Implementation</h3>\n<pre><code class=\"hljs language-sql\"><span class=\"hljs-comment\">-- Increasing Order (Ascending)</span>\n<span class=\"hljs-keyword\">SELECT</span> <span class=\"hljs-operator\">*</span> <span class=\"hljs-keyword\">FROM</span> employees \n<span class=\"hljs-keyword\">ORDER</span> <span class=\"hljs-keyword\">BY</span> salary <span class=\"hljs-keyword\">ASC</span>;\n\n<span class=\"hljs-comment\">-- Decreasing Order (Descending)</span>\n<span class=\"hljs-keyword\">SELECT</span> <span class=\"hljs-operator\">*</span> <span class=\"hljs-keyword\">FROM</span> employees \n<span class=\"hljs-keyword\">ORDER</span> <span class=\"hljs-keyword\">BY</span> salary <span class=\"hljs-keyword\">DESC</span>;\n</code></pre>\n<h3>1. The ORDER BY Clause</h3>\n<p>The <code>ORDER BY</code> clause is used to sort the result-set in ascending or descending order.</p>\n<ul>\n<li><strong>ASC:</strong> (Default) Sorts from lowest to highest (1-10, A-Z).</li>\n<li><strong>DESC:</strong> Sorts from highest to lowest (10-1, Z-A).</li>\n</ul>\n<h3>2. Multi-Level Sorting</h3>\n<p>You can sort by multiple columns. If two employees have the same salary, you can sort them by name.</p>\n<pre><code class=\"hljs language-sql\"><span class=\"hljs-keyword\">SELECT</span> <span class=\"hljs-operator\">*</span> <span class=\"hljs-keyword\">FROM</span> employees \n<span class=\"hljs-keyword\">ORDER</span> <span class=\"hljs-keyword\">BY</span> salary <span class=\"hljs-keyword\">DESC</span>, name <span class=\"hljs-keyword\">ASC</span>;\n</code></pre>\n<h3>3. Other Sorting Keywords/Concepts</h3>\n<ul>\n<li><strong>NULLS FIRST / NULLS LAST:</strong> Specifies where <code>NULL</code> values should appear in the sort.</li>\n<li><strong>Column Index:</strong> You can sort by position (e.g., <code>ORDER BY 2</code>), though this is considered poor practice in production code.</li>\n<li><strong>Limit/Top:</strong> Often used with sorting to find extremes (e.g., \"Top 5 salaries\").</li>\n</ul>\n<pre><code class=\"hljs language-sql\"><span class=\"hljs-comment\">-- MySQL/Postgres</span>\n<span class=\"hljs-keyword\">SELECT</span> <span class=\"hljs-operator\">*</span> <span class=\"hljs-keyword\">FROM</span> employees <span class=\"hljs-keyword\">ORDER</span> <span class=\"hljs-keyword\">BY</span> salary <span class=\"hljs-keyword\">DESC</span> LIMIT <span class=\"hljs-number\">5</span>;\n</code></pre>\n<h3>4. Performance Note</h3>\n<p>Sorting is an <strong>expensive</strong> operation (<span class=\"katex\"><span class=\"katex-mathml\"><math xmlns=\"http://www.w3.org/1998/Math/MathML\"><semantics><mrow><mi>O</mi><mo stretchy=\"false\">(</mo><mi>N</mi><mi>log</mi><mo>⁡</mo><mi>N</mi><mo stretchy=\"false\">)</mo></mrow><annotation encoding=\"application/x-tex\">O(N \\log N)</annotation></semantics></math></span><span class=\"katex-html\" aria-hidden=\"true\"><span class=\"base\"><span class=\"strut\" style=\"height:1em;vertical-align:-0.25em;\"></span><span class=\"mord mathnormal\" style=\"margin-right:0.0278em;\">O</span><span class=\"mopen\">(</span><span class=\"mord mathnormal\" style=\"margin-right:0.109em;\">N</span><span class=\"mspace\" style=\"margin-right:0.1667em;\"></span><span class=\"mop\">lo<span style=\"margin-right:0.0139em;\">g</span></span><span class=\"mspace\" style=\"margin-right:0.1667em;\"></span><span class=\"mord mathnormal\" style=\"margin-right:0.109em;\">N</span><span class=\"mclose\">)</span></span></span></span>). Large tables should ideally be sorted using an <strong>Index</strong> on the column used in <code>ORDER BY</code> to avoid high CPU usage.</p>",
  "rawMarkdown": "\n# SQL Sorting and Ordering\n\nUp to Database Systems Notes\n\n#concept #sql #sorting\n\n## 1. Sorted Data Retrieval\n\n> **Prompt:** \"Write an SQL code to view the records in increasing order of salary, then descending order. Give me explanations of both. What other keywords and commands are there for sorting retrieval of data\"\n> **Lens Applied:** The Optimizationist / The Chief Engineer\n\n### Implementation\n```sql\n-- Increasing Order (Ascending)\nSELECT * FROM employees \nORDER BY salary ASC;\n\n-- Decreasing Order (Descending)\nSELECT * FROM employees \nORDER BY salary DESC;\n```\n\n### 1. The ORDER BY Clause\nThe `ORDER BY` clause is used to sort the result-set in ascending or descending order.\n*   **ASC:** (Default) Sorts from lowest to highest (1-10, A-Z).\n*   **DESC:** Sorts from highest to lowest (10-1, Z-A).\n\n### 2. Multi-Level Sorting\nYou can sort by multiple columns. If two employees have the same salary, you can sort them by name.\n```sql\nSELECT * FROM employees \nORDER BY salary DESC, name ASC;\n```\n\n### 3. Other Sorting Keywords/Concepts\n*   **NULLS FIRST / NULLS LAST:** Specifies where `NULL` values should appear in the sort.\n*   **Column Index:** You can sort by position (e.g., `ORDER BY 2`), though this is considered poor practice in production code.\n*   **Limit/Top:** Often used with sorting to find extremes (e.g., \"Top 5 salaries\").\n```sql\n-- MySQL/Postgres\nSELECT * FROM employees ORDER BY salary DESC LIMIT 5;\n```\n\n### 4. Performance Note\nSorting is an **expensive** operation ($O(N \\log N)$). Large tables should ideally be sorted using an **Index** on the column used in `ORDER BY` to avoid high CPU usage.\n",
  "wordCount": 258,
  "backlinks": [],
  "relativePath": "10_University/Semester_04/Database Systems/Notes/1.1.6 - SQL Sorting and Ordering.md",
  "filename": "1.1.6 - SQL Sorting and Ordering"
}
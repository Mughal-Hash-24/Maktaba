{
  "slug": "os-parallelism-case-study",
  "title": "OS Parallelism Case Study",
  "tags": [
    "field/cs",
    "subject/os",
    "concept/os/parallelism"
  ],
  "htmlContent": "<h1>OS Parallelism Case Study</h1>\n<p>Up to Operating Systems Notes</p>\n<p>#concept #os #parallelism #c-programming</p>\n<h2>1. Parallel Prime Calculator</h2>\n<blockquote>\n<p><strong>Prompt:</strong> \"I am going to write a parallel prime calculator program below. What you'll have to do is go through the whole program and explain to me what's happening and when does each child get created. Draw the tree diagram of parent child using mermaid and prove to me that this is true parallel processing...\"\n<strong>Lens Applied:</strong> The Optimizationist / The Chief Engineer</p>\n</blockquote>\n<h3>1. Code Logic Walkthrough</h3>\n<p>The program implements <strong>Data Parallelism</strong> (SIMD-style logic using processes).</p>\n<ol>\n<li><strong>Setup:</strong> <code>numOfChildren = 4</code>, <code>rangeSize = 100000</code>.</li>\n<li><strong>The Loop (<code>for(int i = 0; i &#x3C; 4; i++)</code>):</strong>\n<ul>\n<li><strong>Iteration 0:</strong> Parent calls <code>fork()</code>. Child 0 created.\n<ul>\n<li><strong>Child 0 Logic:</strong> Calculates primes for range <code>0</code> to <code>100,000</code>. Prints count. Exits.</li>\n<li><strong>Parent Logic:</strong> Continues loop.</li>\n</ul>\n</li>\n<li><strong>Iteration 1:</strong> Parent calls <code>fork()</code>. Child 1 created.\n<ul>\n<li><strong>Child 1 Logic:</strong> Range <code>100,000</code> to <code>200,000</code>.</li>\n</ul>\n</li>\n<li>... (Repeats for 4 children).</li>\n</ul>\n</li>\n<li><strong>The Barrier (<code>wait()</code> Loop):</strong>\n<ul>\n<li>The Parent enters a second loop to <code>wait()</code> for <em>all</em> children. This ensures the main program doesn't exit until all workers are done.</li>\n</ul>\n</li>\n</ol>\n<h3>2. The Process Tree (Mermaid)</h3>\n<p>This is not a linear chain. It is a fan-out structure.</p>\n<pre><code class=\"hljs language-mermaid\">graph TD\n    Parent[Parent Process: Main]\n    Parent -->|fork i=0| C1[Child 0: Range 0-100k]\n    Parent -->|fork i=1| C2[Child 1: Range 100k-200k]\n    Parent -->|fork i=2| C3[Child 2: Range 200k-300k]\n    Parent -->|fork i=3| C4[Child 3: Range 300k-400k]\n\n    C1 -->|exit| OS[OS Kernel]\n    C2 -->|exit| OS\n    C3 -->|exit| OS\n    C4 -->|exit| OS\n\n    OS -->|SIGCHLD + Exit Status| Parent\n</code></pre>\n<h3>3. Proof of Parallelism</h3>\n<p><strong>Why is this \"True Parallel Processing\"?</strong></p>\n<ol>\n<li><strong>Separate PCBs:</strong> The OS treats each of the 4 children as independent schedulable entities.</li>\n<li><strong>Multicore Usage:</strong> If your CPU has 4+ cores, the OS Scheduler will assign Child 0 to Core 1, Child 1 to Core 2, etc. They will physically execute <strong>simultaneously</strong>.</li>\n<li><strong>Speedup:</strong>\n<ul>\n<li><em>Serial Execution:</em> Time(Range 1) + Time(Range 2) + ...</li>\n<li><em>Parallel Execution:</em> MAX(Time(Range 1), Time(Range 2), ...).</li>\n<li>The total wall-clock time will be roughly <span class=\"katex\"><span class=\"katex-mathml\"><math xmlns=\"http://www.w3.org/1998/Math/MathML\"><semantics><mrow><mn>1</mn><mi mathvariant=\"normal\">/</mi><mn>4</mn></mrow><annotation encoding=\"application/x-tex\">1/4</annotation></semantics></math></span><span class=\"katex-html\" aria-hidden=\"true\"><span class=\"base\"><span class=\"strut\" style=\"height:1em;vertical-align:-0.25em;\"></span><span class=\"mord\">1/4</span></span></span></span>th of the serial version (ignoring fork overhead).</li>\n</ul>\n</li>\n</ol>\n<h3>4. Critical Detail: <code>exit(count % 255)</code></h3>\n<ul>\n<li>The child returns the count via <code>exit()</code>.</li>\n<li><strong>Limitation:</strong> The exit status in Unix is an 8-bit integer (0-255). If <code>count</code> > 255, the parent will receive a truncated wrong value (Modulo 256).</li>\n<li><strong>Fix:</strong> For real data passing, use <strong>Pipes</strong> or <strong>Shared Memory</strong>, not exit codes.</li>\n</ul>",
  "rawMarkdown": "\n# OS Parallelism Case Study\n\nUp to Operating Systems Notes\n\n#concept #os #parallelism #c-programming\n\n## 1. Parallel Prime Calculator\n\n> **Prompt:** \"I am going to write a parallel prime calculator program below. What you'll have to do is go through the whole program and explain to me what's happening and when does each child get created. Draw the tree diagram of parent child using mermaid and prove to me that this is true parallel processing...\"\n> **Lens Applied:** The Optimizationist / The Chief Engineer\n\n### 1. Code Logic Walkthrough\nThe program implements **Data Parallelism** (SIMD-style logic using processes).\n\n1.  **Setup:** `numOfChildren = 4`, `rangeSize = 100000`.\n2.  **The Loop (`for(int i = 0; i < 4; i++)`):**\n    *   **Iteration 0:** Parent calls `fork()`. Child 0 created.\n        *   **Child 0 Logic:** Calculates primes for range `0` to `100,000`. Prints count. Exits.\n        *   **Parent Logic:** Continues loop.\n    *   **Iteration 1:** Parent calls `fork()`. Child 1 created.\n        *   **Child 1 Logic:** Range `100,000` to `200,000`.\n    *   ... (Repeats for 4 children).\n3.  **The Barrier (`wait()` Loop):**\n    *   The Parent enters a second loop to `wait()` for *all* children. This ensures the main program doesn't exit until all workers are done.\n\n### 2. The Process Tree (Mermaid)\nThis is not a linear chain. It is a fan-out structure.\n\n```mermaid\ngraph TD\n    Parent[Parent Process: Main]\n    Parent -->|fork i=0| C1[Child 0: Range 0-100k]\n    Parent -->|fork i=1| C2[Child 1: Range 100k-200k]\n    Parent -->|fork i=2| C3[Child 2: Range 200k-300k]\n    Parent -->|fork i=3| C4[Child 3: Range 300k-400k]\n\n    C1 -->|exit| OS[OS Kernel]\n    C2 -->|exit| OS\n    C3 -->|exit| OS\n    C4 -->|exit| OS\n\n    OS -->|SIGCHLD + Exit Status| Parent\n```\n\n### 3. Proof of Parallelism\n**Why is this \"True Parallel Processing\"?**\n1.  **Separate PCBs:** The OS treats each of the 4 children as independent schedulable entities.\n2.  **Multicore Usage:** If your CPU has 4+ cores, the OS Scheduler will assign Child 0 to Core 1, Child 1 to Core 2, etc. They will physically execute **simultaneously**.\n3.  **Speedup:**\n    *   *Serial Execution:* Time(Range 1) + Time(Range 2) + ...\n    *   *Parallel Execution:* MAX(Time(Range 1), Time(Range 2), ...).\n    *   The total wall-clock time will be roughly $1/4$th of the serial version (ignoring fork overhead).\n\n### 4. Critical Detail: `exit(count % 255)`\n*   The child returns the count via `exit()`.\n*   **Limitation:** The exit status in Unix is an 8-bit integer (0-255). If `count` > 255, the parent will receive a truncated wrong value (Modulo 256).\n*   **Fix:** For real data passing, use **Pipes** or **Shared Memory**, not exit codes.\n",
  "wordCount": 421,
  "backlinks": [],
  "relativePath": "10_University/Semester_04/Operating Systems/Notes/1.1.3 - OS Parallelism Case Study.md",
  "filename": "1.1.3 - OS Parallelism Case Study"
}
{
  "slug": "os-pipes-for-ipc",
  "title": "OS Pipes for IPC",
  "tags": [
    "field/cs",
    "#field/cs",
    "#subject/os",
    "#concept/ipc/pipes"
  ],
  "htmlContent": "<p>Up to OS Notes</p>\n<blockquote>\n<p><strong>Prompt:</strong> \"Explain pipes in the scope of inter-process communication. Why is it needed? What problem does it solve. Make a complete C++ showcase for all the functionalities of pipes with explanation of each case and functionality, edge cases.\"\n<strong>Lens Applied:</strong> The Chief Engineer / The Constructivist</p>\n</blockquote>\n<h1>Technical breakdown: Pipes in Inter-Process Communication (IPC)</h1>\n<h2>1. Surgical Definition (Internals)</h2>\n<p>At the kernel level, a <strong>pipe</strong> is a unidirectional FIFO (First-In-First-Out) byte stream implemented as a circular buffer in kernel memory. It solves the problem of <strong>synchronized data exchange</strong> between processes without the overhead of shared memory or the complexity of sockets.</p>\n<ul>\n<li><strong>Anonymous Pipes:</strong> Transient channels created via <code>pipe()</code>. They lack a name in the filesystem and are restricted to related processes (e.g., parent-child) because child processes inherit the file descriptors (FDs).</li>\n<li><strong>Named Pipes (FIFOs):</strong> Persistent filesystem objects created via <code>mkfifo()</code>. They allow unrelated processes to communicate by opening the FIFO path like a file.\nThe kernel manages synchronization: if the pipe is full, the writer blocks; if the pipe is empty, the reader blocks.</li>\n</ul>\n<h2>2. The Laboratory (Proof of Concept)</h2>\n<p>This C++ showcase implements an anonymous pipe between a parent (writer) and child (reader).</p>\n<pre><code class=\"hljs language-cpp\"><span class=\"hljs-meta\">#<span class=\"hljs-keyword\">include</span> <span class=\"hljs-string\">&#x3C;iostream></span></span>\n<span class=\"hljs-meta\">#<span class=\"hljs-keyword\">include</span> <span class=\"hljs-string\">&#x3C;unistd.h></span></span>\n<span class=\"hljs-meta\">#<span class=\"hljs-keyword\">include</span> <span class=\"hljs-string\">&#x3C;sys/wait.h></span></span>\n<span class=\"hljs-meta\">#<span class=\"hljs-keyword\">include</span> <span class=\"hljs-string\">&#x3C;cstring></span></span>\n<span class=\"hljs-meta\">#<span class=\"hljs-keyword\">include</span> <span class=\"hljs-string\">&#x3C;fcntl.h></span></span>\n\n<span class=\"hljs-function\"><span class=\"hljs-type\">int</span> <span class=\"hljs-title\">main</span><span class=\"hljs-params\">()</span> </span>{\n    <span class=\"hljs-type\">int</span> pipefd[<span class=\"hljs-number\">2</span>]; \n    <span class=\"hljs-keyword\">if</span> (<span class=\"hljs-built_in\">pipe</span>(pipefd) == <span class=\"hljs-number\">-1</span>) <span class=\"hljs-keyword\">return</span> <span class=\"hljs-number\">1</span>;\n\n    <span class=\"hljs-type\">pid_t</span> pid = fork();\n    <span class=\"hljs-keyword\">if</span> (pid == <span class=\"hljs-number\">0</span>) {         <span class=\"hljs-comment\">// Child Process (Reader)</span>\n        <span class=\"hljs-built_in\">close</span>(pipefd[<span class=\"hljs-number\">1</span>]);   <span class=\"hljs-comment\">// 1. Close unused write end</span>\n        <span class=\"hljs-type\">char</span> buffer[<span class=\"hljs-number\">128</span>];\n        <span class=\"hljs-type\">ssize_t</span> bytes = <span class=\"hljs-built_in\">read</span>(pipefd[<span class=\"hljs-number\">0</span>], buffer, <span class=\"hljs-built_in\">sizeof</span>(buffer));\n        <span class=\"hljs-keyword\">if</span> (bytes > <span class=\"hljs-number\">0</span>) {\n            buffer[bytes] = <span class=\"hljs-string\">'\\0'</span>;\n            std::cout &#x3C;&#x3C; <span class=\"hljs-string\">\"[Reader] Received: \"</span> &#x3C;&#x3C; buffer &#x3C;&#x3C; std::endl;\n        }\n        <span class=\"hljs-built_in\">close</span>(pipefd[<span class=\"hljs-number\">0</span>]);\n        <span class=\"hljs-keyword\">return</span> <span class=\"hljs-number\">0</span>;\n    } <span class=\"hljs-keyword\">else</span> {                <span class=\"hljs-comment\">// Parent Process (Writer)</span>\n        <span class=\"hljs-built_in\">close</span>(pipefd[<span class=\"hljs-number\">0</span>]);   <span class=\"hljs-comment\">// 2. Close unused read end</span>\n        <span class=\"hljs-type\">const</span> <span class=\"hljs-type\">char</span>* msg = <span class=\"hljs-string\">\"Pipes bypass disk I/O.\"</span>;\n        <span class=\"hljs-built_in\">write</span>(pipefd[<span class=\"hljs-number\">1</span>], msg, <span class=\"hljs-built_in\">strlen</span>(msg)); <span class=\"hljs-comment\">// 3. Kernel copy</span>\n        <span class=\"hljs-built_in\">close</span>(pipefd[<span class=\"hljs-number\">1</span>]);   <span class=\"hljs-comment\">// 4. Closing triggers EOF for reader</span>\n        <span class=\"hljs-built_in\">wait</span>(<span class=\"hljs-literal\">NULL</span>);\n    }\n    <span class=\"hljs-keyword\">return</span> <span class=\"hljs-number\">0</span>;\n}\n</code></pre>\n<h2>3. Memory &#x26; System Context</h2>\n<ul>\n<li><strong>Stack/Heap:</strong> Pipe buffers (typically 64KB) live in <strong>Kernel Space</strong>. Data is copied from the writer’s buffer to the kernel, then to the reader’s buffer.</li>\n<li><strong>Cost:</strong> Highly efficient. No context switching is required for the data itself, only the syscalls (<code>read</code>/<code>write</code>).</li>\n<li><strong>Life-cycle:</strong> Anonymous pipes vanish once all FDs are closed. Named pipes persist on disk but their <em>data</em> is volatile and exists only while processes are connected.</li>\n</ul>\n<h2>4. Best Practices &#x26; Anti-Patterns</h2>\n<ul>\n<li><strong>The \"Broken Pipe\" (SIGPIPE):</strong> Writing to a pipe with no active readers triggers a <code>SIGPIPE</code> signal. If unhandled, the process crashes.</li>\n<li><strong>Partial Reads/Writes:</strong> For pipes, <code>write()</code> is atomic only if the size is <span class=\"katex\"><span class=\"katex-mathml\"><math xmlns=\"http://www.w3.org/1998/Math/MathML\"><semantics><mrow><mo>≤</mo></mrow><annotation encoding=\"application/x-tex\">\\leq</annotation></semantics></math></span><span class=\"katex-html\" aria-hidden=\"true\"><span class=\"base\"><span class=\"strut\" style=\"height:0.7719em;vertical-align:-0.136em;\"></span><span class=\"mrel\">≤</span></span></span></span> <code>PIPE_BUF</code> (4096 bytes on Linux).</li>\n<li><strong>Deadlocks:</strong> If both processes wait to read or write without closing FDs correctly, the system hangs. Always close unused ends immediately after <code>fork()</code>.</li>\n</ul>",
  "rawMarkdown": "Up to OS Notes\n\n> **Prompt:** \"Explain pipes in the scope of inter-process communication. Why is it needed? What problem does it solve. Make a complete C++ showcase for all the functionalities of pipes with explanation of each case and functionality, edge cases.\"\n> **Lens Applied:** The Chief Engineer / The Constructivist\n\n# Technical breakdown: Pipes in Inter-Process Communication (IPC)\n\n## 1. Surgical Definition (Internals)\nAt the kernel level, a **pipe** is a unidirectional FIFO (First-In-First-Out) byte stream implemented as a circular buffer in kernel memory. It solves the problem of **synchronized data exchange** between processes without the overhead of shared memory or the complexity of sockets.\n- **Anonymous Pipes:** Transient channels created via `pipe()`. They lack a name in the filesystem and are restricted to related processes (e.g., parent-child) because child processes inherit the file descriptors (FDs).\n- **Named Pipes (FIFOs):** Persistent filesystem objects created via `mkfifo()`. They allow unrelated processes to communicate by opening the FIFO path like a file.\nThe kernel manages synchronization: if the pipe is full, the writer blocks; if the pipe is empty, the reader blocks.\n\n## 2. The Laboratory (Proof of Concept)\nThis C++ showcase implements an anonymous pipe between a parent (writer) and child (reader).\n\n```cpp\n#include <iostream>\n#include <unistd.h>\n#include <sys/wait.h>\n#include <cstring>\n#include <fcntl.h>\n\nint main() {\n    int pipefd[2]; \n    if (pipe(pipefd) == -1) return 1;\n\n    pid_t pid = fork();\n    if (pid == 0) {         // Child Process (Reader)\n        close(pipefd[1]);   // 1. Close unused write end\n        char buffer[128];\n        ssize_t bytes = read(pipefd[0], buffer, sizeof(buffer));\n        if (bytes > 0) {\n            buffer[bytes] = '\\0';\n            std::cout << \"[Reader] Received: \" << buffer << std::endl;\n        }\n        close(pipefd[0]);\n        return 0;\n    } else {                // Parent Process (Writer)\n        close(pipefd[0]);   // 2. Close unused read end\n        const char* msg = \"Pipes bypass disk I/O.\";\n        write(pipefd[1], msg, strlen(msg)); // 3. Kernel copy\n        close(pipefd[1]);   // 4. Closing triggers EOF for reader\n        wait(NULL);\n    }\n    return 0;\n}\n```\n\n## 3. Memory & System Context\n* **Stack/Heap:** Pipe buffers (typically 64KB) live in **Kernel Space**. Data is copied from the writer’s buffer to the kernel, then to the reader’s buffer.\n* **Cost:** Highly efficient. No context switching is required for the data itself, only the syscalls (`read`/`write`).\n* **Life-cycle:** Anonymous pipes vanish once all FDs are closed. Named pipes persist on disk but their *data* is volatile and exists only while processes are connected.\n\n## 4. Best Practices & Anti-Patterns\n* **The \"Broken Pipe\" (SIGPIPE):** Writing to a pipe with no active readers triggers a `SIGPIPE` signal. If unhandled, the process crashes.\n* **Partial Reads/Writes:** For pipes, `write()` is atomic only if the size is $\\leq$ `PIPE_BUF` (4096 bytes on Linux). \n* **Deadlocks:** If both processes wait to read or write without closing FDs correctly, the system hangs. Always close unused ends immediately after `fork()`.\n",
  "wordCount": 465,
  "backlinks": [],
  "relativePath": "10_University/Semester_04/Operating Systems/Notes/1.1.9 - OS Pipes for IPC.md",
  "filename": "1.1.9 - OS Pipes for IPC"
}